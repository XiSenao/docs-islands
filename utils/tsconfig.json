{
  // Package-local source/editor check. Build lib is tsconfig.lib.build.json.
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "isolatedDeclarations": true,
    "declaration": true,
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    "types": ["node"],
  },
  "include": ["./"],
}
