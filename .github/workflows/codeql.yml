name: CodeQL Security Analysis

env:
  NODE_VERSION: "22"

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  security-events: write

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  analyze:
    name: Analyze Code
    runs-on: ubuntu-latest
    timeout-minutes: 30

    strategy:
      fail-fast: false
      matrix:
        language: ["javascript-typescript"]

    steps:
      # action/checkout@v5.0.0.
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8

      - name: Initialize CodeQL
        # github/codeql-action/init@v4.30.7.
        uses: github/codeql-action/init@e296a935590eb16afc0c0108289f68c87e2a89a5
        with:
          languages: ${{ matrix.language }}
          queries: security-and-quality

      - name: Setup Environment
        uses: ./.github/actions/setup-env
        with:
          node-version: ${{ env.NODE_VERSION }}
          frozen-lockfile: true

      - name: Autobuild
        # github/codeql-action/autobuild@v4.30.7.
        uses: github/codeql-action/autobuild@e296a935590eb16afc0c0108289f68c87e2a89a5

      - name: Perform CodeQL Analysis
        # github/codeql-action/analyze@v4.30.7.
        uses: github/codeql-action/analyze@e296a935590eb16afc0c0108289f68c87e2a89a5
        with:
          category: "/language:${{ matrix.language }}"
          upload: true
