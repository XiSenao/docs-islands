name: CodeQL Security Analysis

env:
  NODE_VERSION: "22"

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  security-events: write

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  analyze:
    name: Analyze Code
    runs-on: ubuntu-latest
    timeout-minutes: 30

    strategy:
      fail-fast: false
      matrix:
        language: ["javascript-typescript"]

    steps:
      # action/checkout@v5.0.0.
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd

      - name: Initialize CodeQL
        # github/codeql-action/init@v4.31.0.
        uses: github/codeql-action/init@014f16e7ab1402f30e7c3329d33797e7948572db
        with:
          languages: ${{ matrix.language }}
          queries: security-and-quality

      - name: Setup Environment
        uses: ./.github/actions/setup-env
        with:
          node-version: ${{ env.NODE_VERSION }}
          frozen-lockfile: true

      - name: Autobuild
        # github/codeql-action/autobuild@v4.31.0.
        uses: github/codeql-action/autobuild@014f16e7ab1402f30e7c3329d33797e7948572db

      - name: Perform CodeQL Analysis
        # github/codeql-action/analyze@v4.31.0.
        uses: github/codeql-action/analyze@014f16e7ab1402f30e7c3329d33797e7948572db
        with:
          category: "/language:${{ matrix.language }}"
          upload: true
