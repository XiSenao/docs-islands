import type { Rollup } from 'vite';

/**
 * Re-export Rollup types for convenience
 */
export type OutputAsset = Rollup.OutputAsset;
export type OutputChunk = Rollup.OutputChunk;
export type RollupOutput = Rollup.RollupOutput;
