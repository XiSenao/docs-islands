diff --git a/dist/node/chunks/dep-D_zLpgQd.js b/dist/node/chunks/dep-D_zLpgQd.js
index 8926328fc7936f695a89773f191a0c9cf7d3d861..f7e3399a9dc0984259b3a5785c585cca12d6619f 100644
--- a/dist/node/chunks/dep-D_zLpgQd.js
+++ b/dist/node/chunks/dep-D_zLpgQd.js
@@ -66920,6 +66920,20 @@ async function bundleConfigFile(fileName, isESM) {
                 }
                 throw e;
               }
+              const isInternalPackage = (resolvedPath) => {
+                // If it's in node_modules, it's definitely an external package
+                if (isInNodeModules(resolvedPath)) {
+                  return false
+                }
+                // If it's not in node_modules, it's likely a linked/internal package
+                // We should not externalize it to allow esbuild to bundle it
+                return true
+              }
+              if (idFsPath && isInternalPackage(idFsPath)) {
+                return {
+                  path: idFsPath,
+                }
+              }
               if (idFsPath && isImport) {
                 idFsPath = pathToFileURL(idFsPath).href;
               }
