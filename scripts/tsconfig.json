{
  "$schema": "https://json.schemastore.org/tsconfig",
  // Root scripts local check. Build leaf: tsconfig.build.json.
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "isolatedDeclarations": false,
    "lib": ["ES2023"],
    "types": ["node"],
  },
}
