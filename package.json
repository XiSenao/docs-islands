{
  "name": "docs-islands-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0",
    "pnpm": ">=10.26.0"
  },
  "workspaces": [
    "packages/*",
    "packages/*/docs",
    "packages/*/playground",
    "packages/plugins/*",
    "packages/**/__tests__/**",
    "docs",
    "utils"
  ],
  "homepage": "https://docs.senao.me/docs-islands",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/XiSenao/docs-islands.git"
  },
  "description": "Cross-framework Islands Architecture for documentation sites",
  "keywords": [
    "docs-islands",
    "vitepress",
    "react",
    "islands-architecture",
    "cross-framework",
    "ssr",
    "hydration",
    "frontend"
  ],
  "scripts": {
    "build": "tsx scripts/build.ts",
    "docs:dev": "sh -c 'pnpm --filter \"@docs-islands/${1:-monorepo}-docs\" docs:dev' --",
    "docs:build": "pnpm run _run docs:build && tsx scripts/merge-docs.ts",
    "docs:preview": "pnpm --filter @docs-islands/monorepo-docs docs:preview",
    "docs:link:dev": "pnpm run _run link:dev",
    "docs:link:prod": "pnpm run _run link:prod",
    "test": "DOCS_ISLANDS_TEST='1' DOCS_ISLANDS_MODE='production' pnpm run build && concurrently --names \"test:unit,test:e2e\" --prefix-colors \"blue,orange\" --kill-others-on-fail \"pnpm run _run test:unit\" \"pnpm run _run test:e2e\"",
    "test:unit": "pnpm run _run test:unit",
    "test:e2e": "pnpm run _run test:e2e",
    "test:smoke": "pnpm run _run smoke vitepress",
    "lint": "eslint . --fix --cache --concurrency auto --config ./eslint.config.mjs && pnpm -r --parallel run lint",
    "lint:packages": "pnpm -r --parallel run lint:package",
    "format": "prettier --write --cache .",
    "typecheck": "tsx scripts/generate-tsconfig-graph-paths.ts --ensure && tsx scripts/check-ts-project-graph.ts && tsc -b tsconfig.graph.json --pretty false && vue-tsc -p docs/tsconfig.json --noEmit && vue-tsc -p packages/vitepress/docs/tsconfig.json --noEmit && vue-tsc -p packages/vitepress/theme/tsconfig.json --noEmit",
    "typecheck:vue": "vue-tsc -p docs/tsconfig.json --noEmit && vue-tsc -p packages/vitepress/docs/tsconfig.json --noEmit && vue-tsc -p packages/vitepress/theme/tsconfig.json --noEmit",
    "tsconfig:graph:paths": "tsx scripts/generate-tsconfig-graph-paths.ts",
    "tsconfig:graph:paths:ensure": "tsx scripts/generate-tsconfig-graph-paths.ts --ensure",
    "tsconfig:graph:check": "tsx scripts/check-ts-project-graph.ts",
    "typecheck:graph": "tsx scripts/generate-tsconfig-graph-paths.ts --ensure && tsc -b tsconfig.graph.json --pretty false",
    "typecheck:graph:verbose": "tsx scripts/generate-tsconfig-graph-paths.ts --ensure && tsc -b tsconfig.graph.json --verbose",
    "typecheck:graph:force": "tsx scripts/generate-tsconfig-graph-paths.ts --ensure && tsc -b tsconfig.graph.json --force --pretty false",
    "typecheck:graph:strict": "tsx scripts/generate-tsconfig-graph-paths.ts --ensure && tsx scripts/check-ts-project-graph.ts && tsc -b tsconfig.graph.json --pretty false",
    "typecheck:graph:lib": "tsx scripts/generate-tsconfig-graph-paths.ts --ensure && tsc -b tsconfig.graph.lib.json --pretty false",
    "typecheck:test": "pnpm -r --parallel run typecheck:test",
    "release": "tsx scripts/release.ts",
    "changelog": "tsx scripts/changelog.ts",
    "changelog:preview": "tsx scripts/changelog.ts --dry-run",
    "clean": "pnpm run _run clean && del-cli scripts/.tsbuild .eslintcache node_modules",
    "_run": "tsx scripts/run-workspace-script.ts",
    "preinstall": "npx only-allow pnpm",
    "postinstall": "tsx scripts/postinstall.ts",
    "prepare": "husky"
  },
  "dependencies": {
    "vite": "catalog:prod",
    "tinyglobby": "catalog:prod"
  },
  "devDependenciesComments": {
    "@playwright/test": "It ensures CI can execute playwright commands."
  },
  "devDependencies": {
    "@types/node": "catalog:dev",
    "@types/prompts": "catalog:dev",
    "eslint": "catalog:lint",
    "@docs-islands/eslint-config": "workspace:*",
    "@docs-islands/logger": "workspace:*",
    "@docs-islands/utils": "workspace:*",
    "prettier": "catalog:format",
    "prettier-plugin-organize-imports": "catalog:format",
    "typescript": "catalog:dev",
    "lint-staged": "catalog:dev",
    "vue-tsc": "catalog:dev",
    "tsx": "catalog:dev",
    "cac": "catalog:dev",
    "prompts": "catalog:dev",
    "del-cli": "catalog:dev",
    "husky": "catalog:dev",
    "concurrently": "catalog:dev",
    "yaml": "catalog:dev",
    "@playwright/test": "catalog:test"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,vue,mjs,cjs}": [
      "eslint --cache --fix"
    ],
    "*": [
      "prettier --write --cache --ignore-unknown"
    ]
  },
  "packageManager": "pnpm@10.26.0"
}
