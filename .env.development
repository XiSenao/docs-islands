# Development mode defaults
# Logs: info, success, warn, error allowed; debug follows inspector
# Sourcemap: enabled
# Minification: disabled
#
# Inherits all base defaults from .env — no overrides needed.
